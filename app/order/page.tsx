'use client';
import {useSearchParams} from 'next/navigation'; import {useState, Suspense} from 'react'; import {products} from '../products';
function OrderForm(){const q=useSearchParams(), initial=q.get('product')||'BRW-001', style=q.get('style')||'', showcase=q.get('showcase')||''; const [done,setDone]=useState<any>(null);const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});setBusy(false);setDone(await r.json())}if(done)return <main className="shell section"><h1>Thank you!</h1><div className="notice"><b>Your order request ID: {done.order_id}</b><p>We have received your request. Please send the same order ID to WhatsApp so Dessert(y) House can confirm availability, price and payment instructions.</p><a className="btn gold" href={done.whatsapp_url}>Continue on WhatsApp</a></div></main>;return <main className="shell section"><div className="eyebrow">ORDER REQUEST</div><h1>Tell us about your celebration.</h1><p className="muted">No payment is collected here. The bakery confirms all prices, delivery and payment manually on WhatsApp.</p><form className="form" onSubmit={submit}>{(style||showcase)&&<div className="notice"><b>Selected reference: {style||showcase}</b><br/>You may change it or explain alterations in the design notes.</div>}<input type="hidden" name="style_code" value={style}/><input type="hidden" name="showcase_code" value={showcase}/><label>Product / product ID</label><select name="product_id" defaultValue={initial}>{products.map(p=><option key={p.id} value={p.id}>{p.id} — {p.name}</option>)}</select><label>Your name</label><input required name="customer_name"/><label>WhatsApp number</label><input required name="phone" inputMode="tel" placeholder="10-digit mobile number"/><label>Celebration / required date</label><input required name="event_date" type="date"/><label>Egg preference</label><select name="egg_preference"><option>Egg</option><option>Eggless</option><option>No preference</option></select><label>Quantity / servings</label><input required name="quantity" placeholder="e.g. 1 kg / 12 cupcakes / serves 15"/><label>Delivery or pickup area</label><input required name="area" placeholder="Chennai locality"/><label>Theme, flavour or design details</label><textarea name="notes" placeholder="Include your design idea. Send inspiration photo on WhatsApp after submitting."/><button className="btn gold" disabled={busy}>{busy?'Saving…':'Submit order request'}</button></form></main>}

export default function Order(){return <Suspense fallback={<main className="shell section">Loading order form…</main>}><OrderForm/></Suspense>}
