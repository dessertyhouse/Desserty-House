'use client';import {useState} from 'react';export default function Reports(){const [pw,setPw]=useState(''),[data,setData]=useState<any>(),[from,setFrom]=useState(new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10)),[to,setTo]=useState(new Date().toISOString().slice(0,10)),[msg,setMsg]=useState('');async function load(){const r=await fetch(`/api/admin/reports?from=${from}&to=${to}`,{headers:{'x-admin-password':pw}}),d=await r.json();d.summary?setData(d):setMsg(d.error||'Unable to load report.')}return <main className="admin-page"><div className="shell"><header className="admin-top"><div><div className="eyebrow">OWNER ONLY · PERFORMANCE REPORTS</div><h1>Orders & worker report</h1></div><a className="btn" href="/admin">← Orders dashboard</a></header><section className="form"><label>Admin password</label><input type="password" value={pw} onChange={e=>setPw(e.target.value)}/><div className="order-edit"><label>From<input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>To<input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label></div><button className="btn gold" onClick={load}>Generate report</button></section>{data&&<><div className="admin-grid"><div className="metric"><span>Total orders</span><strong>{data.summary.total}</strong></div><div className="metric"><span>Completed</span><strong>{data.summary.completed}</strong></div><div className="metric"><span>Open</span><strong>{data.summary.open}</strong></div><div className="metric"><span>Unassigned</span><strong>{data.summary.unassigned}</strong></div></div><section className="manual-box"><h2>Worker performance</h2><table className="table"><thead><tr><th>Worker</th><th>Role</th><th>Assigned</th><th>Completed</th><th>Open</th></tr></thead><tbody>{data.workers.map((w:any)=><tr key={w.worker_id}><td>{w.name}</td><td>{w.role}</td><td>{w.assigned}</td><td>{w.completed}</td><td>{w.open}</td></tr>)}</tbody></table></section></>}{msg&&<p className="notice">{msg}</p>}</div></main>}
