import {notFound} from 'next/navigation';import Link from 'next/link';import {products} from '../../products';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return notFound();return <main><header className="shell nav"><Link className="brand" href="/">Desserty House<small>HANDMADE IN CHENNAI</small></Link><Link className="btn gold" href={`/order?product=${p.id}`}>Order this item</Link></header><section className="shell section"><div className="eyebrow">{p.id} · MADE TO ORDER</div><h1>{p.name}</h1><p className="product-intro">{p.description}</p><div className="photo-grid">{p.gallery.map(style=><div className="style-card" key={style.code}><img src={style.image} alt={`${p.name} ${style.title}`}/><div><b>{style.code} · {style.title}</b><p>{style.description}</p><Link className="text-link" href={`/order?product=${p.id}&style=${style.code}`}>Order this style →</Link></div></div>)}</div><div className="product-info"><div><h2>Made for your moment</h2><ul>{p.details.map(x=><li key={x}>{x}</li>)}</ul></div><aside><b>How to order</b><p>Choose this item, send your date, quantity and preference. We confirm availability, quote and delivery charge personally on WhatsApp.</p><Link className="btn gold" href={`/order?product=${p.id}`}>Request {p.name}</Link></aside></div></section></main>}
