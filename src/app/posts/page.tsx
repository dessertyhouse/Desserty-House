import Link from 'next/link';import {createClient} from '@supabase/supabase-js';export const dynamic='force-dynamic';
export default async function Posts(){const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL||'',process.env.SUPABASE_SERVICE_ROLE_KEY||'');const {data:result}=await db.from('posts').select('*').eq('is_published',true).order('created_at',{ascending:false});const data=result??[];return <main><header className="shell nav"><Link href="/" className="brand">Desserty House<small>HANDMADE IN CHENNAI</small></Link><Link className="btn gold" href="/order">Start an order</Link></header><section className="showcase-hero"><div className="shell"><div className="eyebrow">LATEST FROM DESSERTY HOUSE</div><h1>Offers, launches & sweet news.</h1><p>Fresh updates, seasonal treats and limited celebration offers. Pre-order early on WhatsApp.</p></div></section><section className="shell section">{data.length?<div className="showcase-grid">{data.map((p:any)=><article className="showcase-card" key={p.id}><img src={p.image_url} alt={p.title}/><div><span className="sku">{p.kind?.toUpperCase()||'UPDATE'}</span><h3>{p.title}</h3><p>{p.description}</p><Link className="text-link" href="/order">Ask about this →</Link></div></article>)}</div>:<div className="notice"><b>New offers are coming soon.</b><br/>Follow Dessert(y) House on Instagram or message us on WhatsApp for current availability.</div>}</section></main>}
